# วิธีนำ Lumira ขึ้น Cloudflare

ไฟล์ชุดนี้พร้อมสร้างเป็น **Cloudflare Workers** ซึ่งรองรับการบันทึกผลการนับและการระบุชนิดโดยไม่ต้องใช้ R2 เว็บที่สร้างในหน้า **Pages** ก่อนหน้านี้ใช้กับชุดนี้ไม่ได้ ให้สร้างโปรเจกต์ Worker ใหม่หลังตั้งค่าด้านล่าง

1. นำ **ไฟล์และโฟลเดอร์ทั้งหมดใน ZIP** เข้า GitHub repository `Lolipop881/Lumira` โดยแตก ZIP ก่อน (GitHub ไม่แตกไฟล์ ZIP ให้เอง) อย่าใส่เพียงไฟล์ ZIP หรือ README
2. ใน Cloudflare สร้างฐานข้อมูล **D1** ชื่อ `lumira-db` แล้วคัดลอก `Database ID` ไปแทนเลข `00000000-0000-4000-8000-000000000000` ในไฟล์ `wrangler.jsonc` บน GitHub
3. ให้ดำเนินคำสั่งสร้างตารางฐานข้อมูล 4 บรรทัดตามลำดับใน `README.md` ก่อนทดสอบการบันทึกข้อมูล
4. เชื่อม GitHub repository กับ **Workers** และใช้ Build command `pnpm build` และ Deploy command `pnpm exec wrangler deploy --config dist/server/wrangler.json`
5. เปิด **Cloudflare Access สำหรับทั้งเว็บและ API** และจำกัดสิทธิ์ให้เฉพาะผู้ที่คุณอนุญาต ก่อนนำผลทดลองจริงขึ้นเว็บ ปุ่ม Protect with Access ที่ใช้เฉพาะ preview ไม่พอ

ถ้าต้องการระบุชนิดสิ่งมีชีวิตด้วยบริการภายนอก ให้เพิ่ม API key ของโปรเจกต์ Google AI Studio ที่เป็น Free tier ใน Cloudflare เป็น Secret: `GEMINI_API_KEY` (และ `PLANTNET_API_KEY` หากใช้ระบุพืช) อย่านำ key ลง GitHub

ข้อมูลที่เคยบันทึกในเว็บ Lumira เดิมจะไม่ย้ายมาฐานข้อมูลใหม่นี้โดยอัตโนมัติ

ภาพที่เลือกยังแสดงขณะนับหรือวิเคราะห์ แต่ Lumira จะไม่เก็บภาพลงเซิร์ฟเวอร์ บันทึกเก็บเฉพาะผลจำนวน จุดนับ และผลระบุชนิด หากต้องการเก็บภาพต้นฉบับให้บันทึกไว้ในเครื่องของคุณเอง

## เปิด AI และการแจ้งเตือนโควตา

1. ใน Cloudflare เลือก D1 → `lumira-db` → Console แล้วรัน SQL จาก `drizzle/0003_ai_usage.sql` หนึ่งครั้ง: `CREATE TABLE IF NOT EXISTS ai_usage (day TEXT NOT NULL, model TEXT NOT NULL, count INTEGER NOT NULL DEFAULT 0, PRIMARY KEY (day, model));`
2. สร้าง Gemini API key จากโปรเจกต์ที่ยังเป็น **Free tier** ใน Google AI Studio แล้วไปที่ Worker → Settings → Variables and Secrets → เพิ่ม Secret ชื่อ `GEMINI_API_KEY` อย่าใส่คีย์ใน GitHub
3. ดู Requests per day (RPD) ของโมเดลใน AI Studio แล้วตั้งตัวแปร Worker `AI_DAILY_REQUEST_LIMIT` เป็นตัวเลขที่ต่ำกว่าเพดานจริง เช่นหากแสดง 100 จึงตั้ง 80 (เป็นตัวอย่าง ไม่ใช่โควตาจริงของบัญชี) ถ้าไม่ตั้ง ระบบจะแจ้งว่าประเมินโควตาล่วงหน้าไม่ได้
4. ตัวเลขนี้เป็นการนับคำขอของ Lumira เอง ไม่ใช่ยอดคงเหลือจาก Google; หากบริการแจ้ง 429 ระบบจะลองโมเดลสำรอง การนับรูปจะกลับมาใช้การแยกสีในเครื่องหาก AI ทั้งสองใช้ไม่ได้ ส่วนการระบุชนิดไม่มีโมเดลในเครื่อง

ข้อควรทราบ: หากเปิด Billing ให้โปรเจกต์ Google จะคิดค่าบริการได้ตามการใช้งาน ตรวจสถานะ Free tier ก่อนใส่คีย์; เปิด Cloudflare Access ทั้งเว็บและ API เพื่อไม่ให้ผู้อื่นใช้โควตาโดยไม่ได้รับอนุญาต
