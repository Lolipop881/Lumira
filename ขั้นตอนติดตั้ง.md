# วิธีนำ Lumira ขึ้น Cloudflare

ไฟล์ชุดนี้พร้อมสร้างเป็น **Cloudflare Workers** ซึ่งรองรับการบันทึกผลและรูปภาพ เว็บที่สร้างในหน้า **Pages** ก่อนหน้านี้ใช้กับชุดนี้ไม่ได้ ให้สร้างโปรเจกต์ Worker ใหม่หลังตั้งค่าด้านล่าง

1. นำ **ไฟล์และโฟลเดอร์ทั้งหมดใน ZIP** เข้า GitHub repository `Lolipop881/Lumira` โดยแตก ZIP ก่อน (GitHub ไม่แตกไฟล์ ZIP ให้เอง) อย่าใส่เพียงไฟล์ ZIP หรือ README
2. ใน Cloudflare สร้างฐานข้อมูล **D1** ชื่อ `lumira-db` แล้วคัดลอก `Database ID` ไปแทนเลข `00000000-0000-4000-8000-000000000000` ในไฟล์ `wrangler.jsonc` บน GitHub
3. ใน Cloudflare สร้างที่เก็บภาพ **R2** ชื่อ `lumira-images` หาก Cloudflare ขอให้เปิดใช้ R2 หรือยืนยันข้อมูลชำระเงิน ต้องทำในบัญชีของคุณเอง
4. ให้ดำเนินคำสั่งสร้างตารางฐานข้อมูล 3 บรรทัดตามลำดับใน `README.md` ก่อนทดสอบการบันทึกข้อมูล
5. เชื่อม GitHub repository กับ **Workers** และใช้ Build command `pnpm build` และ Deploy command `pnpm exec wrangler deploy --config dist/server/wrangler.json`
6. เปิด **Cloudflare Access สำหรับทั้งเว็บและ API** และจำกัดสิทธิ์ให้เฉพาะผู้ที่คุณอนุญาต ก่อนนำผลทดลองจริงขึ้นเว็บ ปุ่ม Protect with Access ที่ใช้เฉพาะ preview ไม่พอ

ถ้าต้องการระบุชนิดสิ่งมีชีวิตด้วยบริการภายนอก ให้เพิ่ม API key ใน Cloudflare เป็น Secret: `PLANTNET_API_KEY` หรือ `OPENAI_API_KEY` ตามบริการที่ใช้ อย่านำ key ลง GitHub

ข้อมูลและรูปที่เคยบันทึกในเว็บ Lumira เดิมจะไม่ย้ายมาฐานข้อมูลใหม่นี้โดยอัตโนมัติ
